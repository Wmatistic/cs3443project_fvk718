<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#101010">

    <Button
        android:id="@+id/routineButton"
        android:layout_width="0dp"
        android:layout_height="120dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="10dp"
        android:backgroundTint="#232222"
        android:text="Name"
        android:textAlignment="center"
        app:cornerRadius="20px"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/startWorkoutButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="10dp"
        android:backgroundTint="#005B54"
        android:text="Start Workout"
        android:textSize="16sp"
        app:cornerRadius="20px"
        app:layout_constraintBottom_toBottomOf="@+id/routineButton"
        app:layout_constraintEnd_toEndOf="@+id/routineButton"
        app:layout_constraintStart_toStartOf="@+id/routineButton" />

    <TextView
        android:id="@+id/workoutNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:text="TextView"
        android:textColor="#FFFFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="@+id/routineButton"
        app:layout_constraintTop_toTopOf="@+id/routineButton" />

    <ImageView
        android:id="@+id/deleteWorkoutImage"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        app:layout_constraintEnd_toEndOf="@+id/routineButton"
        app:layout_constraintTop_toTopOf="@+id/routineButton"
        app:srcCompat="@drawable/trash_can" />
</androidx.constraintlayout.widget.ConstraintLayout>